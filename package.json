{
  "name": "cours1_webfront",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/atordjmann/cours1_webfront.git",
  "author": "atordjmann <tordjmann.audrey@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build.sh": "rm -rf dist && mkdir dist && cp src/index.html dist && yarn run postcss -o dist/main.css src/tailwind.pcss",
    "build.bat": "del dist\\* ; copy src/index.html dist; yarn run postcss -o dist/main.css src/tailwind.pcss"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^1.3.0",
    "postcss": "^7.0.18",
    "postcss-cli": "^6.1.3",
    "postcss-import": "^12.0.1",
    "tailwindcss": "^1.1.2"
  }
}
